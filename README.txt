version 2 has the following fixes:
gets path to all scripts from passed variable  >> yes
input path to reference as variable  >> wip
make it so nchr is taken from the input plink file X
in folder called breed_stats
	make dog breed %s print out in a text document for each dog 
	